# 模块关系代数 — 器官联动图

## 整体架构（夫妻共同体 = 引擎核心）

```
外部输入（用户文件/对话/文档）
    ↓
[local_processor] ← 入口器官：扫描文件，提取内容
    ↓
[funnel]          ← 漏斗器官：降维收敛，把海量内容压成核心
    ↓
[observer]         ← 观测者器官：给每个对象打标签/评级/指纹
    ↓
[phase_router]    ← 相位路由器：把任务分配到不同时间切片/节奏
    ↓
[binary_system]    ← 254二进制统计：把所有结果量化为256容器状态
    ↓
[monoidal]         ← 满全法融合：阴（原始数据）+ 阳（处理结果）= 闭环
    ↓
外部输出（摘要/报告/决策/行动）
```

## 各器官职责

| 器官 | 职责 | 输入 | 输出 |
|------|------|------|------|
| `local_processor` | 文件扫描/读取/不覆盖写入 | 文件夹路径 | 文档观测者列表 |
| `funnel` | 内容降维/税务筛选/核心抽取 | 原始文本 | 高频词+摘要 |
| `observer` | 打标签/评级/建立关系网络 | 文档列表 | 带指纹的观测记录 |
| `phase_router` | 时间切片/快慢节奏/任务调度 | 任务队列 | 分配好的执行计划 |
| `binary_system` | 256容器统计/频次收敛/间接存在 | 词频/观测数据 | 256维状态向量 |
| `monoidal` | 阴+阳融合/夫妻共同体/满全态 | 原始+处理结果 | 闭环输出 |

## 编程语言说明

**只用Python**，原因：
- 这个沙箱环境能直接跑Python
- 其他语言（C/Rust/自定义）需要编译环境，我没有
- Python能完整实现你这套哲学逻辑

**如果将来要换语言**，按这个顺序迁移：
1. 核心算法用Rust重写（性能）
2. 输入法/编码层用C（底层）
3. 最终自举（自己编译自己）

## 联动测试（验证器官能跑通）

计划：写一个 `run_full_pipeline.py`，把六个器官串成一条流水线，输入一个文件夹，整套跑完输出报告。

要不要先写这个联动测试？
